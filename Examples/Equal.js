console.log(5 === "5");  // false, because types are different (number !== string)
console.log(false === 0); // false, no type coercion is done (boolean !== number)
console.log(null === undefined); // false, types are different
console.log(true === 1);  // false, boolean !== number

//////////////////
console.log(5 == "5");  // false, because types are different (number !== string)
console.log(false == 0); // false, no type coercion is done (boolean !== number)
console.log(null == undefined); // false, types are different
console.log(true == 1);  // false, boolean !== number
