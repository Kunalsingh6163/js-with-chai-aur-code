
const array = [ 55, 21, 14, 44, 74, 245, 88];

// sort in assending order
const sortArr = array.sort((a,b)=>{return a - b});
console.log(sortArr);

//sort in descending order
const sortArrDesce = array.sort((a,b)=>{return b - a});
console.log(sortArrDesce);


// using for loop

function sorting(){
    
}